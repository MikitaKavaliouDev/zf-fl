# E2E tests use Patrol and require a device/emulator + running backend.
# They are tagged 'e2e' and skipped by default in `flutter test`.
# Run with: flutter test --tags e2e
# Or use: patrol test
tags:
  e2e:
    skip: "Use `patrol test` to run E2E tests (requires device + backend)"
